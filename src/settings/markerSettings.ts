type MarkerSettingsType = {
    DEFAULT_TEXT_SIZE: string
    DEFAULT_WIDTH: number
    MIN_WIDTH: number
}

const MARKER_SETTINGS: MarkerSettingsType = {
    DEFAULT_TEXT_SIZE: '20',
    DEFAULT_WIDTH: 100,
    MIN_WIDTH: 50
};

export default MARKER_SETTINGS;
