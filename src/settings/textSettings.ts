export type TextSettingsType = {
    DEFAULT_MARKER_TEXT_SIZE: string
    DEFAULT_PRESCRIPTION_MARKER_TEXT_SIZE: string
    MAX_TEXT_SIZE: number
    
}

const TEXT_SETTINGS: TextSettingsType = {
    DEFAULT_MARKER_TEXT_SIZE: '20',
    DEFAULT_PRESCRIPTION_MARKER_TEXT_SIZE: '20',
    MAX_TEXT_SIZE: 100
};

export default TEXT_SETTINGS;
