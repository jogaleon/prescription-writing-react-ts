import PrescriptionMarkerData from './../types/state/prescriptionMarkerData';
type PrescriptionMarkerSettingsType = {
    DEFAULT_WIDTH: number
    DEFAULT_HEIGHT: number
    MIN_WIDTH: number
    MIN_HEIGHT: number
}

const PRESCRIPTION_MARKER_SETTINGS: PrescriptionMarkerSettingsType = {
    DEFAULT_WIDTH: 100,
    DEFAULT_HEIGHT: 100,
    MIN_WIDTH: 50,
    MIN_HEIGHT: 50,
};

export default PRESCRIPTION_MARKER_SETTINGS;
